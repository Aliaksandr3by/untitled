<major-version>.<minor-version>.<incremental-version>-qualifier
the major, minor, and incremental values are numeric, and the qualifier can
have values such as rC, alpha, beta, and snapshot. some examples that
follow this convention are 1.0.0, 2.4.5-snapshot, 3.1.1-rC1, and so forth.

mvn plugin_identifier:goal_identifier
clean:clean: clean before the colon (:) represents the clean plug-in, and the clean following the colon represents the clean goal.

mvn help:describe -Dplugin=exec

mvn dependency:analyze
mvn dependency:analyze-duplicate
mvn dependency:resolve
mvn dependency:resolve-plugin
mvn dependency:tree
mvn dependency:list
mvn dependency:copy-dependencies

mvn help:evaluate "-Dexpression=settings.localRepository"

mvn versions:display-plugin-updates

https://www.mojohaus.org/versions-maven-plugin/dependency-updates-report.html

mvn versions:display-dependency-updates

mvn -D allowMajorUpdates=true -D allowMinorUpdates=false -D allowIncrementalUpdates=false -D allowSnapshots=false  versions:display-dependency-updates


mvn package –Dmaven.test.skip=true


java -cp testJava-1.0-SNAPSHOT.jar com.example.mainApp.main
